spring:
  application:
    name: Movies-service
  cloud:
    config:
      uri: http://localhost:9050/ParcialTwo/v1
      username: ${ConfigUser}
      password: ${ConfigPassword}
      enabled: true
