spring:
  application:
    name: Bookings-service
  cloud:
    config:
      uri: http://localhost:9050/ParcialTwo/v1
      username: ${ConfigUser}
      password: ${ConfigPassword}
      enabled: true